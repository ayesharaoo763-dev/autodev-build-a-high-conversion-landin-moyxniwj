const navbar = document.querySelector('.navbar'); const hamburger = document.querySelector('.hamburger'); const navLinks = document.querySelector('.nav-links'); hamburger.addEventListener('click', () => { navLinks.classList.toggle('active'); }); gsap.from('.header', { duration: 1, y: 100, opacity: 0, ease: 'power2.inOut' }); gsap.from('.features', { duration: 1, y: 100, opacity: 0, ease: 'power2.inOut', scrollTrigger: { trigger: '.features', start: 'top center' } }); gsap.from('.pricing', { duration: 1, y: 100, opacity: 0, ease: 'power2.inOut', scrollTrigger: { trigger: '.pricing', start: 'top center' } }); gsap.from('.about', { duration: 1, y: 100, opacity: 0, ease: 'power2.inOut', scrollTrigger: { trigger: '.about', start: 'top center' } });